#!/usr/bin/env python3

#-->Takes input as 'number or strings' and gives output as boolen value either true or false
#-->to find out the ASCII of any character use
#   ord('a')
#-->you can find the particuler charcter using the ASCII code
#   chr(45)

'''
    x > y
    x < y
    x == y
    x != y
    x >= y
    x <= y


'''

# strings comparision based upon ASCII numbers
#   ord('a') > ord('b')
'''
    'a' > 'b'
    'a' < 'b'
    'a' != 'b'
    'a' == 'b'
    'a' <= 'b'
    'a' >= 'b'

    'abc' > 'acb'           (in this type of scenario operration will be performed character by character)
'''

#@ lacture 3
